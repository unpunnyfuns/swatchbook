---
'@unpunnyfuns/swatchbook-blocks': minor
---

Blocks default their color format to `Config.defaultColorFormat`, and now accept a `colorFormat` prop that overrides both an outer `ColorFormatContext` and that project default for that block.
