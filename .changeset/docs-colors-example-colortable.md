---
'@unpunnyfuns/swatchbook-core': patch
---

Add `<ColorTable>` to the colors example in the authoring guide and to the "Blocks at a glance" table. The example now shows `ColorPalette` + `ColorTable` + `TokenTable` together — the three natural shapes for a colors doc page (swatch grid, variant-aware grouped table, flat list).
