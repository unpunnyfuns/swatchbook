---
'@unpunnyfuns/swatchbook-addon': patch
---

Docs: add README for the `@unpunnyfuns/swatchbook-integrations` package — covers the two shipping subpaths (`/tailwind` and `/css-in-js`), install + wiring pattern, the `SwatchbookIntegration` contract for custom integrations, and explicit scope boundaries (display-side only, no production artifact writes, MUI-shape deferred).
