---
"@unpunnyfuns/swatchbook-blocks": patch
---

Docs: describe the axis-variance indicator and toolbar behavior generically over the project's own axes, rather than presenting the dogfood's `mode`/`brand`/`contrast` as the canonical set
