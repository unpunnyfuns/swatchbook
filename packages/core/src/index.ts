export { defineSwatchbookConfig } from '#/config';
export { loadProject, resolveTheme } from '#/load';

export type {
  Config,
  Diagnostic,
  DiagnosticSeverity,
  Project,
  ResolvedTheme,
  Theme,
  ThemeConfig,
  TokenMap,
} from '#/types';
