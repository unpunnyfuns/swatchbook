import { bench, describe } from 'vitest';

describe('token-graph (placeholder — fleshed out in Phase 5)', () => {
  bench('placeholder', () => {
    // intentionally empty — Phase 5 adds real benchmarks for
    // buildTokenGraph + resolveAt against reference, stress, and
    // real-consumer fixtures.
  });
});
